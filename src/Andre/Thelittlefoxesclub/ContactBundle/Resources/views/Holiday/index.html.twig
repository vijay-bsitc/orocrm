{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set pageTitle = 'Holiday'|trans %}

{% block navButtons %}
    {% if is_granted('contact.holiday_create') %}
        <div class="btn-group">
            {{ UI.addButton({
            'path': path('contact.holiday_create'),
            'entity_label': 'Holiday'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}