{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set pageTitle = 'Weekly'|trans %}

{% block navButtons %}
    {% if is_granted('contact.weekly_create') %}
        <div class="btn-group">
            {{ UI.addButton({
            'path': path('contact.weekly_create'),
            'entity_label': 'Weekly'|trans
            }) }}
        </div>
    {% endif %}
{% endblock %}